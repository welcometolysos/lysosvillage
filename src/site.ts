/**
 * While the site is being built we ask search engines to stay away.
 * ONE LINE TO LAUNCH: set this to false, save, and the site becomes
 * findable on Google. Nothing else needs changing.
 */
export const HIDE_FROM_SEARCH = true;
